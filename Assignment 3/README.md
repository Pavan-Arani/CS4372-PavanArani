# Assignment 3 – Image Classification with CNNs

## Overview
This project performs **image classification** using a **Convolutional Neural Network (CNN)** with **transfer learning**. The dataset contains images organized into `train` and `test` folders with corresponding labels in `labels.csv`. The pre-trained **MobileNetV2** model is fine-tuned for this dataset.

---

## Requirements
- Python 3.10+  
- TensorFlow / Keras 3  
- NumPy, Pandas, Matplotlib  

Install dependencies:

```bash
pip install tensorflow pandas matplotlib
